Run on the Target Server:
```sh
curl -s https://raw.githubusercontent.com/kellerflint/grc-vm-setup/refs/heads/main/vm-setup-305.sh > install.sh && chmod 777 install.sh && . ./install.sh
```

If you need to update the URL (for instance in the case of a change in the file name), copy the link to the raw .sh file in GitHub. To get there, click into the file on GitHub and click the "Raw" button. Copy the URL to this page. Test